{
  "name": "@lynjs/test",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "",
  "scripts": {
    "test": "npm run build --workspace=@lynjs/core && npm run test:run",
    "test:run": "vitest run",
    "test:watch": "npm run build --workspace=@lynjs/core && concurrently --kill-others-on-fail --names core,test \"npm run build --workspace=@lynjs/core -- --watch\" \"vitest --watch\""
  },
  "dependencies": {
    "@lynjs/core": "0.0.0"
  },
  "devDependencies": {
    "@vitest/browser-playwright": "^4.1.11",
    "playwright": "^1.62.1",
    "vitest": "^4.1.11"
  }
}
