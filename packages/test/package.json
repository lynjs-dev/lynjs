{
  "name": "@lynjs/test",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "",
  "scripts": {
    "pretest": "npm run build --workspace=@lynjs/core && npm run build",
    "build": "tsc -p tsconfig.json",
    "test": "npm run build --workspace=@lynjs/core && npm run build && npm run test:run",
    "test:run": "node dist/index.js"
  },
  "dependencies": {
    "@lynjs/core": "latest"
  }
}
