2. **`REQUIREMENTS.md` — 요구사항 정의**

- LynJS가 반드시 제공해야 하는 기능
- 비기능 요구사항
- 이식성, 성능, DX, 호환성 기준
- 초기 버전의 범위와 비범위
- “무엇을 만족하면 성공인가”를 구체화
  이 문서를 가장 먼저 작성하는 것을 권합니다.

3. **`DESIGN_PRINCIPLES.md` — 설계 원칙**

- UI Component의 Ownership
- 웹 표준을 어디까지 우선할 것인지
- Framework abstraction을 언제 허용할 것인지
- Fine-grained update를 어떤 원칙으로 적용할지
- Public API와 내부 구현의 분리
- “기술 선택이 충돌할 때 무엇을 우선하는가”를 정리

4. **`PROGRAMMING_MODEL.md` — LynJS 프로그래밍 모델**

- 사용자 관점에서 LynJS Component란 무엇인가
- State / Property / Event / Lifecycle / View의 관계
- Component 간 통신
- 상태 소유권
- Side Effect의 위치
- 예제 API
  여기서 처음으로 `LynElement`, `@state`, `@element`, JSX 같은 구체적인 형태를 본격적으로 다루면 됩니다.

5. **`ARCHITECTURE.md` — 전체 아키텍처**

- Core Runtime
- Reactivity
- Rendering Layer
- Custom Elements Integration
- Compiler/Transform
- Tooling
- SSR/HMR 확장 지점
  이 문서에서야 `dom-expressions` 같은 구체적인 구현 기술이 주요하게 등장합니다.

6. **`REACTIVITY.md` — Reactivity 설계**

- State 변경 추적
- Dependency
- Effect
- Computed
- Scheduling / batching
- Cleanup
- Component Lifecycle과 Reactive Scope 관계

7. **`COMPONENT_MODEL.md` — Component 모델**

- `LynElement`
- 생성 / 연결 / 해제 / 재연결
- State와 Property 구분
- Attribute reflection
- Event
- Child component
- Ownership
- 상속 정책

8. **`RENDERING.md` — Rendering 설계**

- JSX가 어떤 역할을 하는지
- Static / Dynamic binding
- Conditional rendering
- List rendering
- DOM identity
- `dom-expressions`와 LynJS 간 경계
  여기서는 “왜 dom-expressions를 사용한다”보다 **어디까지 맡기고 LynJS는 무엇을 책임지는지**가 중요합니다.

9. **`API_DESIGN.md` — Public API 정의**

- `LynElement`
- `element`
- `state`
- `property`
- 향후 `effect`, `computed` 등
- API naming rule
- decorator 정책
- breaking change 기준

10. **`ROADMAP.md` — 개발 단계**

- Phase 0 PoC
- Core Runtime
- Component Model
- Rendering Integration
- HMR
- SSR
- CLI
- Docs
- 0.1 / 0.5 / 1.0 기준

11. **`DECISIONS/` — ADR(Architecture Decision Records)**
    예를 들어:

```text
docs/decisions/
├─ 0001-use-custom-elements.md
├─ 0002-use-dom-expressions.md
├─ 0003-use-decorators.md
├─ 0004-state-property-semantics.md
└─ 0005-no-virtual-dom.md
```
