**⚠️ No public API is available yet.**  
This package is the root of the monorepo, published early to reserve the channel and package name.

- JS transformation/bundling: not applied  
- JS files: none for now (public API will be added in future versions)  
- TypeScript: configuration work in progress  

Planned for upcoming versions:
- Public API (`import { LynElement } from 'lynjs'`)  
- Declaration files (`types/`) and documentation  

